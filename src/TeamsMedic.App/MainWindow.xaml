<Window x:Class="TeamsMedic.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Teams Medic"
        MinHeight="760"
        MinWidth="1080"
        Height="820"
        Width="1180"
        Background="#F5F7FA">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="MinHeight" Value="58" />
            <Setter Property="Margin" Value="0,0,12,12" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="#176B87" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#324B5C" />
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Normal" />
        </Style>
        <Style x:Key="Badge" TargetType="Border">
            <Setter Property="Background" Value="#E8EEF4" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,0,8,8" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#16232E" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Teams Medic" FontSize="34" FontWeight="Bold" />
                <TextBlock Text="A local repair toolkit for Microsoft Teams desktop issues"
                           FontSize="16"
                           Margin="0,4,0,0"
                           Foreground="#52616D" />
                <TextBlock Text="Not affiliated with Microsoft. Runs locally. Safe repairs only by default."
                           FontSize="13"
                           Margin="0,8,0,0"
                           Foreground="#6B7782" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <CheckBox Content="Dry run"
                          IsChecked="{Binding DryRun}"
                          Margin="0,0,16,0"
                          VerticalAlignment="Center" />
                <Button Content="Refresh status"
                        Command="{Binding RefreshDetectionCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1.35*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0,0,18,0">
                    <WrapPanel Margin="0,0,0,10">
                        <Border Style="{StaticResource Badge}">
                            <TextBlock Text="{Binding NewTeamsStatus}" />
                        </Border>
                        <Border Style="{StaticResource Badge}">
                            <TextBlock Text="{Binding ClassicTeamsStatus}" />
                        </Border>
                        <Border Style="{StaticResource Badge}">
                            <TextBlock Text="{Binding WebView2Status}" />
                        </Border>
                        <Border Style="{StaticResource Badge}">
                            <TextBlock Text="{Binding AdminStatus}" />
                        </Border>
                    </WrapPanel>

                    <UniformGrid Columns="2" Margin="0,0,0,12">
                        <Button Content="Quick Repair" Command="{Binding QuickRepairCommand}" Style="{StaticResource ActionButton}" />
                        <Button Content="Guided Fix: Calls Not Showing" Command="{Binding CallsNotShowingCommand}" Style="{StaticResource ActionButton}" />
                        <Button Content="Guided Fix: Notifications" Command="{Binding NotificationsCommand}" Style="{StaticResource ActionButton}" />
                        <Button Content="Guided Fix: Freezes or Crashes" Command="{Binding FreezesCommand}" Style="{StaticResource ActionButton}" />
                        <Button Content="Guided Fix: Will Not Start" Command="{Binding WillNotStartCommand}" Style="{StaticResource ActionButton}" />
                        <Button Content="Create IT Report" Command="{Binding CreateReportCommand}" Style="{StaticResource ActionButton}" />
                    </UniformGrid>

                    <CheckBox Content="Refresh notification area by restarting Windows Explorer during Quick Repair"
                              IsChecked="{Binding RefreshNotificationArea}"
                              Margin="0,0,0,18" />

                    <TextBlock Text="Web test result" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ComboBox SelectedValue="{Binding WebTestResult, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="Content"
                              Margin="0,0,0,14">
                        <ComboBoxItem Content="Not entered" />
                        <ComboBoxItem Content="Web call works: desktop app/local Windows issue" />
                        <ComboBoxItem Content="Web call fails: likely account, tenant, Teams policy, or company configuration" />
                    </ComboBox>

                    <TextBlock Text="User notes for IT report" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox Text="{Binding UserNotes, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="82"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,16" />

                    <Button Content="Advanced Tools"
                            Command="{Binding ShowAdvancedCommand}"
                            Style="{StaticResource SecondaryButton}"
                            HorizontalAlignment="Left" />

                    <StackPanel Visibility="{Binding ShowAdvancedWarning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                Margin="0,12,0,0">
                        <TextBlock Text="Advanced tools" FontSize="18" FontWeight="Bold" Margin="0,0,0,8" />
                        <UniformGrid Columns="2">
                            <Button Content="Clear Teams cache only" Command="{Binding FullCacheResetCommand}" Style="{StaticResource SecondaryButton}" />
                            <Button Content="Restart Explorer" Command="{Binding RestartExplorerCommand}" Style="{StaticResource SecondaryButton}" />
                            <Button Content="Open New Teams cache" Command="{Binding OpenNewCacheCommand}" Style="{StaticResource SecondaryButton}" />
                            <Button Content="Open Classic Teams cache" Command="{Binding OpenClassicCacheCommand}" Style="{StaticResource SecondaryButton}" />
                            <Button Content="Open notification settings" Command="{Binding OpenNotificationSettingsCommand}" Style="{StaticResource SecondaryButton}" />
                            <Button Content="Open Apps settings" Command="{Binding OpenAppsSettingsCommand}" Style="{StaticResource SecondaryButton}" />
                        </UniformGrid>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="Progress log" FontSize="18" FontWeight="Bold" DockPanel.Dock="Left" />
                    <Button Content="Copy log"
                            Command="{Binding CopyLogCommand}"
                            Style="{StaticResource SecondaryButton}"
                            MinHeight="34"
                            Padding="12,6"
                            Margin="12,0,0,0"
                            DockPanel.Dock="Right" />
                </DockPanel>

                <Border Grid.Row="1" Background="#0F1720" CornerRadius="6" Padding="10">
                    <ListBox ItemsSource="{Binding Logs}"
                             Background="#0F1720"
                             Foreground="#D7E2EA"
                             BorderThickness="0"
                             FontFamily="Consolas"
                             FontSize="12"
                             HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="#D7E2EA" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <TextBlock Grid.Row="2"
                           Text="{Binding StatusMessage}"
                           Margin="0,10,0,0"
                           Foreground="#52616D" />
            </Grid>
        </Grid>

        <TextBlock Grid.Row="2"
                   Margin="0,14,0,0"
                   Foreground="#6B7782"
                   Text="Teams support logs can be collected manually with Ctrl + Alt + Shift + 1 in Teams or from the Teams system tray icon." />
    </Grid>
</Window>
